# Security

Do not file secrets in GitHub issues. Email is not set up yet. Use a private GitHub security advisory on this repository.

Keys belong in the Computer secret store. They must never appear in `model-bindings.json`, git history, screenshots, or chat logs.

If you find a way for OpenBot to log `Authorization` headers or write keys into bindings, open an advisory.
